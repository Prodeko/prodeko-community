version: '3.4'
services:
  talks-cms:
    ports:
      - 8055:8055
    restart: unless-stopped

  talks-web:
    build:
      network: prodeko-docker-net
    restart: unless-stopped

networks:
  default:
    external:
      name: prodeko-docker-net
